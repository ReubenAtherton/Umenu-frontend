- add styling to handle the image issues inside the restaurant page *DONE*
- add more data to the db (python), add descriptions and allergies to show inside the individual meal info window 
- add splash screen with logo?
- add reviews section (this will probably need a user account which is another step)
- 
- improve home page * 1/2 DONE*
- add more restaurants
- try to make it more realistic
- add more restaurants and create a recommended list so that the app isnt loading in every single restaurant 
    on start
- make it so when no data is fetched, there is a loading screen for every api dependent data.